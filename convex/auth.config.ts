//https://credible-urchin-52.clerk.accounts.dev

//https://rich-doe-39.clerk.accounts.dev/.well-known/jwks.json

const authConfig = {
  providers: [
    {
      domain: "https://rich-doe-39.clerk.accounts.dev",
      applicationID: "convex",
    },
  ]
};

export default authConfig;